name: validate-bicep

on:
  pull_request:
  push:
    branches: [main]

jobs:
  file-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f bicep/main.bicep && test -f docs/parity-matrix.md
      - run: echo "Placeholder for az bicep build/lint validation"
